//variables.py
string -- str
numeric---int, float, complex
sequence---list, tuple, range, set, dictionaries
mapping-- dict(key:value,)
boolean type--boolean

list is a mutable datatype means we can make changes inside the list
string[]is a immutable datatype as we can only access characters from the string 
but we cannot make any changes no addition, no deletion , no replacement as we do in list[]

#todays agenda:

if and elif and else;

for loop programs